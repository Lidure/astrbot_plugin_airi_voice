{
  "extra_voice_pool": {
    "description": "额外语音文件池（网页上传）",
    "hint": "上传 mp3/wav 等文件，文件名（去后缀）作为触发关键词。支持多文件上传。",
    "type": "file",
    "default": []
  },
  "trigger_mode": {
    "description": "触发模式",
    "type": "string",
    "hint": "prefix: 必须用 #voice 前缀；direct: 直接输入关键词",
    "default": "direct",
    "options": ["direct", "prefix"]
  }
}
