# 🌸 更新日志 (Changelog)

所有重要的项目更新都将记录在此文件中。

---

## [Unreleased]

## [2.11.0] - 2026-09-08

### 新增

- 新增 `case_insensitive_keyword_match` 配置，默认关闭。
- 默认保持关键词大小写敏感；开启后主关键词、额外关键词的冲突检查与运行时触发均不区分英文字母大小写，例如 `test`、`Test`、`TEST` 会被视为同一个关键词。
- WebUI 关键词管理遇到冲突等 400 错误时，现在会显示后端返回的中文原因，不再只显示 `Request failed with status code 400`。

### 优化

- 关键词冲突提示会明确说明冲突的两个关键词，以及是否因为开启了“忽略关键词大小写”导致冲突。
- WebUI 上传说明同步为单个文件最大 50 MB。

## [2.10.2] - 2026-09-04

### 变更

- 将 `/voice.add` 与 WebUI 的单个语音文件上传上限统一从 20 MB 提高到 50 MB。
- 保持原始音频格式保存与既有发送行为不变。

## [2.10.1] - 2026-09-02

### 变更

- 新增 `convert_audio_to_wav`（发送前转换为 WAV）配置，默认关闭。关闭时恢复旧版原始格式直发行为；开启时才使用 v2.10.0 引入的 WAV 兼容层与 `send_cache` 缓存。
- 保留 WAV 兼容层作为针对 `ffmpeg not found`、部分非 WAV 音频发送失败等环境问题的可选兼容方案。

## [2.10.0] - 2026-09-02

### 新增

- 新增可选的 `fuzzy_keyword_match`（模糊匹配）配置：开启后用户消息包含任意主关键词或额外关键词即可触发；精确匹配优先，否则选择最长命中关键词；Prefix/LLM/随机语音边界保持不变。
- WebUI 增加“音频管理 / 关键词管理”双导航。
- 支持为每条音频配置多个额外触发关键词；主关键词与别名均可触发同一音频。
- 关键词别名持久化到插件数据目录 `keyword_aliases.json`，并对主关键词/别名执行忽略大小写的全局唯一校验。
- 列表与随机语音继续只使用主关键词，避免别名造成重复展示或改变随机权重。

### 修复

- 修复合法主关键词/别名以“随机”开头时会被随机命令解析抢占的问题；未加 `/` 的精确已存在关键词现在优先作为语音关键词，`随机语音` 等保留命令及显式 `/随机...` 行为不变。
- 修复部分音频名称正确但平台发送失败的问题：所有聊天语音发送统一经过 WAV 兼容层，依次支持 AstrBot MediaResolver、PyAV 自带编解码回退、系统 ffmpeg 兜底与 SILK 解码，并按源文件版本缓存转换结果。
- 修复 WebUI 添加额外关键词时报 `Plugin bridge endpoint is invalid.`：别名新增/删除改为固定 Plugin Pages endpoint，并通过 POST JSON body 传递参数。
- 上传语音后在上传表单附近显示明确的成功/失败反馈；成功时展示对应主关键词并说明列表已刷新。

### 依赖

- 新增 `av>=17.1.0,<19`（PyAV），用于系统未安装 ffmpeg 时仍可将常见音频格式转换为 WAV；PyPI 提供 Linux x86_64 与 ARM64/aarch64 预编译 wheel。

## [2.9.9] - 2026-09-02

### 修复

- 修复 WebUI 上传音频时报 `Request failed with status code 404` 的问题。
- Plugin Pages 上传改为官方 bridge 约定的固定 `voices/upload` endpoint，不再把关键词拼进动态上传路径。
- WebUI 会将用户填写的关键词写入待上传文件名；后端在固定上传路由下可从文件名恢复关键词，同时保留原有表单、查询参数和旧动态路由兼容。

## [2.9.8] - 2026-08-26

### 修复

- 修复 `/随机...` 同一消息被多个 AstrBot 处理器同时命中时一次返回两条语音的问题；同一事件现在只允许一个随机语音处理器完成发送。
- WebUI 允许有管理权限的用户删除插件内置 `voices/` 音频，同时继续保护由配置直接引用的外部语音文件。
- 修复 `.silk`（例如内置“打卡啦摩托”）无法在 WebUI 试听的问题：服务端使用 `silk-python` 解码为 24 kHz 单声道 WAV 后再交给浏览器播放。

### 依赖

- 新增 `silk-python>=0.2.8`，用于跨平台 Silk 试听解码。
