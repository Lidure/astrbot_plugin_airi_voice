# 🌸 更新日志 (Changelog)

所有重要的项目更新都将记录在此文件中。

---

## [Unreleased]

## [2.10.2] - 2026-09-04

### 变更

- 将 `/voice.add` 与 WebUI 的单个语音文件上传上限统一从 20 MB 提高到 50 MB。
- 保持原始音频格式保存与既有发送行为不变。

## [2.10.1] - 2026-09-02

### 变更

- 新增 `convert_audio_to_wav`（发送前转换为 WAV）配置，默认关闭。关闭时恢复旧版原始格式直发行为；开启时才使用 v2.10.0 引入的 WAV 兼容层与 `send_cache` 缓存。
- 保留 WAV 兼容层作为针对 `ffmpeg not found`、部分非 WAV 音频发送失败等环境问题的可选兼容方案。

## [2.10.0] - 2026-09-02

### 新增

- 新增可选的 `fuzzy_keyword_match`（模糊匹配）配置：开启后用户消息包含任意主关键词或额外关键词即可触发；精确匹配优先，否则选择最长命中关键词；Prefix/LLM/随机语音边界保持不变。
- WebUI 增加“音频管理 / 关键词管理”双导航。
- 支持为每条音频配置多个额外触发关键词；主关键词与别名均可触发同一音频。
- 关键词别名持久化到插件数据目录 `keyword_aliases.json`，并对主关键词/别名执行忽略大小写的全局唯一校验。
- 列表与随机语音继续只使用主关键词，避免别名造成重复展示或改变随机权重。

### 修复

- 修复合法主关键词/别名以“随机”开头时会被随机命令解析抢占的问题；未加 `/` 的精确已存在关键词现在优先作为语音关键词，`随机语音` 等保留命令及显式 `/随机...` 行为不变。
- 修复部分音频名称正确但平台发送失败的问题：所有聊天语音发送统一经过 WAV 兼容层，依次支持 AstrBot MediaResolver、PyAV 自带编解码回退、系统 ffmpeg 兜底与 SILK 解码，并按源文件版本缓存转换结果。
- 修复 WebUI 添加额外关键词时报 `Plugin bridge endpoint is invalid.`：别名新增/删除改为固定 Plugin Pages endpoint，并通过 POST JSON body 传递参数。
- 上传语音后在上传表单附近显示明确的成功/失败反馈；成功时展示对应主关键词并说明列表已刷新。

### 依赖

- 新增 `av>=17.1.0,<19`（PyAV），用于系统未安装 ffmpeg 时仍可将常见音频格式转换为 WAV；PyPI 提供 Linux x86_64 与 ARM64/aarch64 预编译 wheel。

## [2.9.9] - 2026-09-02

### 修复

- 修复 WebUI 上传音频时报 `Request failed with status code 404` 的问题。
- Plugin Pages 上传改为官方 bridge 约定的固定 `voices/upload` endpoint，不再把关键词拼进动态上传路径。
- WebUI 会将用户填写的关键词写入待上传文件名；后端在固定上传路由下可从文件名恢复关键词，同时保留原有表单、查询参数和旧动态路由兼容。

## [2.9.8] - 2026-08-26

### 修复

- 修复 `/随机...` 同一消息被多个 AstrBot 处理器同时命中时一次返回两条语音的问题；同一事件现在只允许一个随机语音处理器完成发送。
- WebUI 允许有管理权限的用户删除插件内置 `voices/` 音频，同时继续保护由配置直接引用的外部语音文件。
- 修复 `.silk`（例如内置“打卡啦摩托”）无法在 WebUI 试听的问题：服务端使用 `silk-python` 解码为 24 kHz 单声道 WAV 后再交给浏览器播放。

### 依赖

- 新增 `silk-python>=0.2.8`，用于跨平台 Silk 试听解码。

## [2.9.7] - 2026-08-04

### 优化

- 将“上传新语音”移动到“浏览语音”上方，方便快速上传。

## [2.9.6] - 2026-08-04

### 修复

- 移除 mini 播放器中无实际必要的关闭按钮。
- 修复音频播放结束后再次点击播放无效的问题，现在会从头重新播放。

## [2.9.5] - 2026-08-04

### 修复

- 修复 mini 播放器按钮横向挤压导致展开和关闭按钮不可见的问题。
- mini 播放器改为标题与功能按钮分层布局，并增加按钮可用空间。

## [2.9.4] - 2026-08-04

### 优化

- mini 播放器隐藏进度条，仅保留小型播放/暂停按钮。
- 缩小 mini 播放器字体、按钮和最大宽度，减少对页面的遮挡。

## [2.9.3] - 2026-08-04

### 优化

- 移除大尺寸展开播放器。
- 新增右侧 mini 播放器，可展开为底部紧凑播放器，再次收起可回到 mini 模式。

## [2.9.2] - 2026-08-04

### 优化

- 播放器默认使用紧凑模式，支持展开和收起，减少对页面内容的遮挡。
- 重新整理上传语音区域的卡片、提示和操作布局。

## [2.9.1] - 2026-08-04

### 修复与优化

- 修复 Plugin Pages 使用不存在的 `apiDelete()` 导致删除语音失败的问题。
- 新增 POST 删除兼容路由，保留原有删除权限校验。
- 播放器和错误提示改为页面底部悬浮显示。
- 重新整理上传语音区域的桌面双列和移动端单列布局。

## [2.9.0] - 2026-08-04

### Added

- Added a shared voice preview player with pause and seek controls.
- Automatically switches the active player when another voice preview starts.
- Refreshed the voice management page visuals and playback status presentation.

## [2.8.10] - 2026-08-04

### 修复

- 兼容 AstrBot bridge 返回 JSON 文本字符串而非已解析对象的情况，修复试听数据被误判为无效的问题。

## [2.8.9] - 2026-08-04

### 修复

- 试听数据改用十六进制传输，彻底规避 Base64 与 Plugin Pages bridge 的编码兼容问题。

## [2.8.8] - 2026-08-04

### 修复

- 使用专用 `audio_base64` 字段返回试听数据，避免与 AstrBot 错误响应的通用 `data` 字段冲突。

## [2.8.7] - 2026-08-04

### 修复

- 试听接口遇到错误响应时不再把错误文本当作 Base64 音频解码。
- 增加 Base64 格式校验，提供更明确的试听错误信息。

## [2.8.6] - 2026-08-04

### 修复

- 兼容 AstrBot 不同版本对试听 JSON 音频数据的多层响应包装。

## [2.8.5] - 2026-08-03

### 修复

- 兼容不同 AstrBot 版本对 Plugin Pages JSON 响应的包装格式，修复试听提示“音频数据无效”。

## [2.8.4] - 2026-08-03

### 修复

- 修复 AstrBot Plugin Pages 中 `apiGet()` 不支持 `.blob()` 导致试听失败的问题。
- 试听接口改为返回 Base64 音频 JSON，由网页转换为 Blob 播放，不再触发 IDM 下载。

## [2.8.3] - 2026-08-03

### 修复

- 修复网页端试听被识别为下载的问题，试听接口改为浏览器内联音频响应。

## [2.8.2] - 2026-08-03

### 修复

- 修复 Plugin Pages 使用错误路由前缀导致网页端提示“未找到路由”。
- 修复 `extra_voice_pool` 仅接受 `extra_voices` 子目录的问题，兼容插件数据目录内的相对路径和绝对路径。

## [2.8.1] - 2026-08-03

### 修复

- 修复 AstrBot 以 `data.plugins.<插件名>.main` 包方式加载插件时，无法导入插件内部模块的问题。

## [2.8.0] - 2026-08-03

- WebUI 上传继续支持历史 `.silk` / `.amr` 格式，并补充支持 `.flac` / `.m4a`；Plugin Page 入口位于 `pages/airi-voice/index.html`。

### ✨ WebUI 语音管理 MVP

- 新增基于安全语音目录的 Dashboard 路由和管理页面，支持目录浏览、搜索、筛选、试听、上传、删除与重载。
- 新增 Plugin Pages / Web API 兼容回退：不支持的 AstrBot 版本仍保留聊天命令和配置页管理。
- 新增明确的 `webui_admin_users` WebUI 管理员用户名列表；在 `admin_mode: admin` 下，Dashboard 变更操作必须匹配已认证用户名。
- 路径安全校验、重复关键词拒绝、内置语音不可删除和管理员变更限制已覆盖 WebUI 操作。
- 保持原有触发语义：普通关键词与 `随机语音` 直接触发；开启随机前缀后仅 `/随机关键词` 触发，`//随机关键词` 不触发。

## [2.7] - 2026-07-31

### 修复

- 修复开启 `enable_prefix` 后 `/随机` 命令被 AstrBot 命令解析拦截的问题。
- 仅限制随机语音使用 `/` 前缀，其他关键词触发方式不变。

## [2.6] - 2026-06-5

### 🐛 bug修复

-**针对voice.add的修复** 
  - 日前，voice.add命令添加的语音是极度失真压缩的，我一直没有发现这个问题，感谢[Whereis-Alice](https://github.com/Whereis-Alice)的issue。
  - 通过url获取的音频已经是被压缩的了，本插件直接获取了本地缓存ori的ptt中音频，并加以处理，使add的语音保持原音频质量
  

## [2.4 & 2.5] - 2026-05-25

### ✨ 新增功能

-**图片渲染** 
  - 对voice.list和voice.help进行了图片渲染
  - 需要安装 Pillow 库，已经添加了依赖安装。如果失败，需要在平台日志右上角的安装pip库，安装 Pillow 即可
    
## [2.3] - 2026-03-20

### ✨ 新增功能
-**🤖 bot追加语音（开关）** 
  - bot 回复的文本只要包含任意语音关键词，就会自动追加对应语音
  - 默认为发送文本中的检测到的第一个关键词对应的语音（且只发送一个）
  - 修复了，发送voice.list后，追加语音
    
如果有其他想法，欢迎issue和pr

---
## [2.2] - 2026-03-14

### ✨ 新增功能

- **🤖 LLM 触发模式（可选）**  
  - 在插件配置中为 `trigger_mode` 新增 `llm` 选项。  
  - 仅当触发模式为 `llm` 时，才会向 AstrBot 注册 `airi_list_all_voices` / `airi_search_voices` / `airi_send_voice` 等 LLM 工具。  
  - 未开启 `llm` 模式时，插件行为与旧版完全一致，只通过关键词触发语音，不参与 LLM 工具调用。

- **🧠 LLM 语音选择工具**  
  - 新增语音工具：  
    - `airi_list_all_voices`：列出当前可用语音名称，供大模型选择。  
    - `airi_search_voices`：按关键词模糊搜索语音名称（可通过 `llm_select_mode` 配置启用）。  
    - `airi_send_voice`：根据指定名称向当前会话发送对应语音。  
  - 工具返回结果主要供大模型内部使用，对用户尽量不增加额外文本噪音。

- **🎲 随机语音指令**  
  - 支持在聊天中直接发送：  
    - `随机发条语音` / `随机语音`：全局随机发送一条语音。  
    - `随机XX`：在包含 `XX` 的语音中随机挑选一条发送。

---
## [2.1] - 2026-03-13
- **🆕 Bug修复**  
  修复了通过voice.add添加的语音无法持久保留的问题。
  现在重启插件也不会被清除了。

## [2.0] - 2026-03-13

### ✨ 新增功能

- **🆕 引用添加语音**  
  新增 `/voice.add 名字` 命令，支持在聊天中引用语音消息快速添加新语音
  - 无需进入后台，聊天中即可完成语音添加
  - 自动下载并保存到持久化数据目录
  - 添加后立即可用，无需重启或手动刷新

- **🗑️ 删除语音**  
  新增 `/voice.delete 名字` 命令，支持删除通过 `/voice.add` 添加的语音
  - 保护本地 `voices/` 目录文件不被误删
  - 自动同步更新配置池

- **⚙️ 配置自动持久化**  
  通过 `/voice.add` 添加的语音会自动加入 `extra_voice_pool` 配置
  - 机器人重启后语音列表依然保留
  - 无需手动编辑配置文件

### 🔒 安全改进

- 所有管理命令 (`voice.add` / `voice.delete` / `voice.reload`) 均需要管理员权限
- 路径安全检查，防止目录穿越攻击
- 只允许删除 `extra_voices/` 目录下的文件

### 📁 存储优化

- 网页上传和 `/voice.add` 添加的文件统一存储至数据目录
- 确保插件更新时用户添加的语音不会丢失

---

## [1.5] - 2026-3-11

### ✨ 新增功能

- **🔐 权限控制系统**  
  新增管理员权限验证，支持三种模式：
  - `all`: 所有人可用
  - `admin`: 仅管理员可用
  - `whitelist`: 白名单用户可用（默认）

- **❓ 权限检查命令**  
  新增 `/voice.check` 命令，方便调试权限问题

- **🔄 配置热更新**  
  自动检测网页配置变化，无需重启即可刷新语音列表
  
### 📝 上架了Abstrbot插件仓库

### 🐛 Bug 修复

- 修复用户 ID 提取在某些平台上的兼容性问题
- 修复语音列表排序不一致的问题
- 修复文件路径解析错误

### 📝 文档更新

- 完善 README.md 使用说明
- 添加权限配置示例

---

## [1.4] - 2026-3-10

### ✨ 新增功能

- **📄 分页列表**  
  `/voice.list` 支持分页查看，每页最多 25 条语音

- **🌐 网页配置支持**  
  支持在 AstrBot 网页后台配置额外语音路径

- **🔁 手动刷新**  
  新增 `/voice.reload` 命令，手动重新加载语音列表

### 🐛 Bug 修复

- 修复长语音列表显示不完整的问题
- 修复特殊字符文件名无法识别的问题

---

##  [1.3] - 2026-3-9

### ✨ 新增功能

- **🎯 触发模式切换**  
  支持 `direct` (直接触发) 和 `prefix` (前缀触发) 两种模式

- **❓ 帮助命令**  
  新增 `/voice.help` 命令，显示使用说明

### 🐛 Bug 修复

- 修复语音文件加载失败时无提示的问题
- 修复空关键词导致的异常

---

##  [1.2] - 2026-3-8

### ✨ 重大更新

- **🏗️ 架构重构**  
  适配 AstrBot 最新插件框架 API
  - 使用 `StarTools` 获取规范数据目录
  - 使用 `event.get_sender_id()` 提取用户 ID
  - 兼容多平台消息格式

- **📁 双目录支持**  
  支持 `voices/` 本地目录和 `extra_voices/` 数据目录

### 🐛 Bug 修复

- 修复旧版本 API 兼容性问题
- 修复路径解析错误

---

## [1.0.0] -  2026-3-8

### ✨ 首次发布

- 基础语音触发功能
- 本地 `voices/` 目录扫描
- 关键词匹配发送语音


## 🔗 相关链接

- 📦 [GitHub 仓库](https://github.com/Lidure/astrbot_plugin_airi_voice)
- 🐛 [问题反馈](https://github.com/Lidure/astrbot_plugin_airi_voice/issues)
- 📖 [使用文档](https://github.com/Lidure/astrbot_plugin_airi_voice#readme)

---

<div align="center">

**Made with 💕 by [lidure](https://github.com/Lidure)**  
📜 遵循 MIT 协议开源

</div>
